export const environment = {
  production: false,
  supabaseUrl: '',
  supabaseAnonKey: '',
  apiBase: 'http://localhost:3000/api',
  /** Bypass auth when Supabase keys are not configured; real auth used once keys are set */
  devPreview: true,
};
